# 🤖 Telegram Bot Setup - Трекер малыша

## Описание

Это серверная часть Telegram бота для приложения "Трекер малыша". Бот приветствует пользователей и предоставляет кнопку для открытия веб-приложения.

## Возможности бота

- 👋 Приветственное сообщение при команде `/start`
- 🚀 Кнопка для открытия веб-приложения
- ❓ Справка по использованию
- 📖 Информация о приложении
- 🔔 В будущем: отправка уведомлений по расписанию

## Установка

### 1. Установите зависимости

```bash
npm install
```

или

```bash
yarn install
```

### 2. Создайте бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Baby Tracker")
   - Введите username бота (должен заканчиваться на `bot`, например: "my_baby_tracker_bot")
4. Скопируйте полученный токен

### 3. Настройте веб-приложение в боте

После создания бота, настройте веб-приложение:

```
/setdescription - установите описание бота
/setabouttext - установите текст "О боте"
```

Пример описания:
```
Трекер малыша — удобное приложение для отслеживания активностей вашего ребенка. 
Кормление, сон, прогулки, рост и многое другое!
```

### 4. Создайте файл `.env`

Скопируйте `.env.bot.example` в `.env` и заполните:

```bash
cp .env.bot.example .env
```

Отредактируйте `.env`:

```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
WEB_APP_URL=https://your-app-url.vercel.app
```

**Важно:**
- `BOT_TOKEN` - токен от @BotFather
- `WEB_APP_URL` - URL вашего деплоя на Vercel/Netlify/др.

### 5. Запустите бота

**Для разработки:**
```bash
npm run dev
```

**Для продакшена:**
```bash
npm start
```

## Деплой на сервер

### Вариант 1: VPS (Ubuntu/Debian)

1. Установите Node.js:
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

2. Клонируйте проект и установите зависимости:
```bash
git clone your-repo
cd your-repo
npm install
```

3. Создайте `.env` файл с вашими данными

4. Используйте PM2 для запуска:
```bash
sudo npm install -g pm2
pm2 start bot.js --name baby-tracker-bot
pm2 startup
pm2 save
```

### Вариант 2: Railway.app

1. Создайте аккаунт на [Railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Добавьте переменные окружения:
   - `BOT_TOKEN`
   - `WEB_APP_URL`
5. Railway автоматически деплоит приложение

### Вариант 3: Heroku

1. Создайте Procfile:
```
web: node bot.js
```

2. Деплой:
```bash
heroku create your-baby-tracker-bot
heroku config:set BOT_TOKEN=your_token
heroku config:set WEB_APP_URL=your_url
git push heroku main
```

### Вариант 4: Render.com

1. Создайте аккаунт на [Render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите репозиторий
4. Настройте:
   - Environment: Node
   - Build Command: `npm install`
   - Start Command: `node bot.js`
5. Добавьте переменные окружения

## Структура проекта

```
.
├── bot.js              # Основной файл бота
├── package.json        # Зависимости для бота
├── .env                # Конфигурация (не коммитить!)
├── .env.bot.example    # Пример конфигурации
└── README-BOT.md       # Эта инструкция
```

## Команды бота

- `/start` - Приветствие и кнопка открытия приложения
- `/help` - Подробная справка по использованию
- Обычные сообщения - Предложение открыть приложение

## Расширение функционала

### Добавление уведомлений

В будущем можно добавить функционал отправки уведомлений:

```javascript
// Пример отправки уведомления
async function sendNotification(chatId, message) {
  await bot.sendMessage(chatId, message, {
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '📊 Открыть приложение',
            web_app: { url: WEB_APP_URL }
          }
        ]
      ]
    }
  });
}
```

Для этого нужно будет:
1. Сохранять chat_id пользователей в Supabase
2. Настроить cron-задачи для проверки напоминаний
3. Отправлять уведомления через bot API

### Добавление команд

Добавьте новые команды в `bot.js`:

```javascript
bot.onText(/\/stats/, async (msg) => {
  const chatId = msg.chat.id;
  // Получить статистику из базы данных
  // Отправить пользователю
});
```

## Troubleshooting

### Ошибка "Polling error"

Проверьте:
- Правильность токена бота
- Интернет-соединение
- Не запущен ли бот в другом месте

### Бот не отвечает

1. Проверьте логи
2. Убедитесь, что бот запущен
3. Проверьте токен в `.env`

### Кнопка веб-приложения не работает

1. Убедитесь, что `WEB_APP_URL` указан правильно
2. Проверьте, что приложение задеплоено и доступно
3. URL должен быть с `https://`

## Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь, что все переменные окружения настроены
3. Проверьте документацию Telegram Bot API

## Лицензия

MIT
